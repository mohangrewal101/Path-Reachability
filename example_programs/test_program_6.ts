function example3(a: number, b: number, c: number) {
    if (((c > a) || (a <= b)) && !((b == c) || (b > c))) {
        // do something
    } else {
        // do something else

    }

    if ((a > 100)) {
        // do something
        if (c > 90) {
            // do something
        } else {
            // do something else
        }
    } else {
        if (a < 90) {
            // do something
        }
    }
}
