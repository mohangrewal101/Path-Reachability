function isWeekend(isWeekend: boolean, isHoliday: boolean) {
  if (isWeekend || isHoliday) {
    // do something here
    if (isWeekend && isHoliday) {
      // do something else
    }

    if (!isHoliday) {
      // do something else
    }
  }
}
